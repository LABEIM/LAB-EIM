---
import TimelineSteps from './TimelineSteps.astro';

interface Props {
  initialStage: string;
  technicalTestResultsCfg: {
    title: string;
    subtitle: string;
    newsUrl?: string;
    documentUrl?: string;
  };
}

const { initialStage, technicalTestResultsCfg } = Astro.props;
---

<div id="view-technical-test-results" class={initialStage === 'technical_test_results' ? '' : 'is-hidden'}>
  <div class="registration-header">
    <h2>{technicalTestResultsCfg.title}</h2>
    <p class="registration-hero-subtitle">{technicalTestResultsCfg.subtitle}</p>
  </div>

  <TimelineSteps activeStepIndex={3} markCurrentAsCompleted={true} />

  {/* NIM Search Box for Technical Test Results */}
  <div class="search-lookup-box">
    <h3 class="search-lookup-title">
      <i class="fa-solid fa-magnifying-glass reg-icon-mr-sm"></i> Check Technical Test Result
    </h3>
    <div class="search-lookup-form">
      <input type="text" id="search-tech-nim-input" class="form-control reg-input-bg" placeholder="Enter Student ID (NIM)..." />
      <button id="search-tech-nim-btn" class="btn btn-primary reg-btn-search">
        Search <i class="fa-solid fa-search reg-icon-ml-xs"></i>
      </button>
    </div>
    <div id="search-tech-result-box" class="search-result-display"></div>
  </div>

  <div class="reg-btn-wrapper">
    <a href="/" class="btn btn-secondary reg-btn-padded">
      <i class="fa-solid fa-house reg-btn-icon-left"></i>Back to Home
    </a>
  </div>
</div>


